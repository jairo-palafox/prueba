#-------------------------------------------------------------------------------
# File: ret_disponibilidad_ley73.4gl
# GENERATED BY fglwsdl 1454432468
#-------------------------------------------------------------------------------
# THIS FILE WAS GENERATED. DO NOT MODIFY.
#-------------------------------------------------------------------------------


IMPORT FGL WSHelper
IMPORT com
IMPORT xml


GLOBALS "ret_disponibilidad_ley73.inc"



#-------------------------------------------------------------------------------
# Service: retiroSaldosDisponiblesLey73
# Port:    retiroSaldosDisponiblesLey73PortType
# Server:  http://10.90.8.132/disponibilidadLey73/ws/r/retiroSaldosDisponiblesLey73
#-------------------------------------------------------------------------------

PRIVATE DEFINE fn_ret_saldos_disponibles_ley73HTTPReq     com.HTTPRequest
PRIVATE DEFINE fn_ret_saldos_disponibles_ley73HTTPResp    com.HTTPResponse

#-------------------------------------------------------------------------------

#
# Operation: fn_ret_saldos_disponibles_ley73
#
#
# FUNCTION: fn_ret_saldos_disponibles_ley73_g
#   RETURNING: soapStatus
#   INPUT: GLOBAL fn_ret_saldos_disponibles_ley73Request
#   OUTPUT: GLOBAL fn_ret_saldos_disponibles_ley73Response
#
FUNCTION fn_ret_saldos_disponibles_ley73_g()
  DEFINE wsstatus   INTEGER
  DEFINE retryAuth  INTEGER
  DEFINE retryProxy INTEGER
  DEFINE retry      INTEGER
  DEFINE nb         INTEGER
  DEFINE uri        STRING
  DEFINE setcookie  STRING
  DEFINE mustUnderstand INTEGER
  DEFINE request    com.HTTPRequest
  DEFINE response   com.HTTPResponse
  DEFINE writer     xml.StaxWriter
  DEFINE reader     xml.StaxReader

  #
  # INIT VARIABLES
  #
  LET wsstatus = -1
  LET retryAuth = FALSE
  LET retryProxy = FALSE
  LET retry = TRUE
  LET uri = com.WebServiceEngine.GetOption("SoapModuleURI")

  IF retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Address.Uri IS NULL THEN
    IF ( FGL_GETENV("URL_RETWS_DISPONIBILIDAD_LEY73") ) THEN
        LET retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Address.Uri = FGL_GETENV("URL_RETWS_DISPONIBILIDAD_LEY73")
    ELSE
        DISPLAY "ERROR: No se encuentra la variable de entorno URL_RETWS_DISPONIBILIDAD_LEY73 o esta vacia. Se usara la URL http://10.90.8.132/disponibilidadLey73/ws/r/retiroSaldosDisponiblesLey73"
        LET retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Address.Uri = "http://10.90.8.132/disponibilidadLey73/ws/r/retiroSaldosDisponiblesLey73"
    END IF

  END IF

  #
  # CREATE REQUEST
  #
  TRY
    LET request = com.HTTPRequest.Create(retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Address.Uri)
    CALL request.setMethod("POST")
    CALL request.setCharset("UTF-8")
    CALL request.setHeader("SOAPAction","\"fn_ret_saldos_disponibles_ley73\"")
    CALL WSHelper_SetRequestHeaders(request, retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.Request.Headers)
    IF retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.Version IS NOT NULL THEN
      CALL request.setVersion(retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.Version)
    END IF
    IF retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.Cookie IS NOT NULL THEN
      CALL request.setHeader("Cookie",retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.Cookie)
    END IF
    IF retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.ConnectionTimeout <> 0 THEN
      CALL request.setConnectionTimeout(retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.ConnectionTimeout)
    END IF
    IF retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.ReadWriteTimeout <> 0 THEN
      CALL request.setTimeout(retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.ReadWriteTimeout)
    END IF
    IF retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.CompressRequest IS NOT NULL THEN
      CALL request.setHeader("Content-Encoding",retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.CompressRequest)
    END IF
    CALL request.setHeader("Accept-Encoding","gzip, deflate")
  CATCH
    LET wsstatus = STATUS
    CALL WSHelper_FillSOAP11WSError("Client","Cannot create HTTPRequest")
    RETURN wsstatus    
  END TRY

  # START LOOP
  WHILE retry
    LET retry = FALSE

    #
    # Stax request
    #
    TRY
      LET writer = request.beginXmlRequest()
      CALL WSHelper_WriteStaxSOAP11StartEnvelope(writer)
      CALL WSHelper_WriteStaxSOAP11StartBody(writer)
      #
      # STAX SOAP REQUEST SERIALIZE
      #
      CALL xml.Serializer.VariableToStax(fn_ret_saldos_disponibles_ley73Request,writer)
      CALL WSHelper_WriteStaxSOAP11EndBody(writer)
      CALL WSHelper_WriteStaxSOAP11EndEnvelope(writer)
      CALL request.endXmlRequest(writer)
    CATCH
      LET wsstatus = STATUS
      CALL WSHelper_FillSOAP11WSError("Client",SQLCA.SQLERRM)
      RETURN wsstatus    
    END TRY

    #
    # PROCESS RESPONSE
    #
    TRY
      LET response = request.getResponse()

      #
      # RETRIEVE SERVICE SESSION COOKIE
      #
      LET setcookie = response.getHeader("Set-Cookie")
      IF setcookie IS NOT NULL THEN
        LET retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.Cookie = WSHelper_ExtractServerCookie(setcookie,retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Address.Uri)
      END IF

      #
      # RETRIEVE HTTP RESPONSE Headers
      #
      CALL WSHelper_SetResponseHeaders(response, retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.Response.Headers)
      CASE response.getStatusCode()

        WHEN 500 # SOAP Fault
          #
          # STAX SOAP FAULT
          #
          LET reader = response.beginXmlResponse() # Begin Streaming Response
          IF NOT WSHelper_ReadStaxSOAP11StartEnvelope(reader) THEN
            EXIT CASE
          END IF
          # Process SOAP headers 
          IF WSHelper_CheckStaxSOAP11Header(reader) AND NOT reader.isEmptyElement() THEN
            CALL reader.nextTag()
            WHILE (reader.getEventType()=="START_ELEMENT")
              IF WSHelper_CheckStaxSOAP11HeaderActor(reader,uri) THEN
                LET mustUnderstand = WSHelper_GetStaxSOAP11HeaderMustUnderstand(reader)
                IF mustUnderstand = -1 THEN
                  CALL WSHelper_FillSOAP11WSError("Client","Invalid mustUnderstand value")
                  EXIT CASE
                END IF
                IF mustUnderstand THEN
                  CALL WSHelper_FillSOAP11WSError("MustUnderstand","Mandatory header block not understood")
                  EXIT CASE
                ELSE
                  CALL reader.nextSibling() # Skip header, not necessary
                END IF
              ELSE
                CALL reader.nextSibling() # Skip header, not intended to us
              END IF
            END WHILE
            IF NOT WSHelper_CheckStaxSOAP11Header(reader) THEN
              CALL WSHelper_FillSOAP11WSError("Client","No ending header tag found")
              EXIT CASE
            ELSE
              CALL reader.nextTag()
            END IF
          ELSE
            IF WSHelper_CheckStaxSOAP11Header(reader) THEN
              CALL reader.nextSibling() # Skip SOAP headers
            END IF
          END IF
          IF NOT WSHelper_ReadStaxSOAP11StartBody(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11StartFault(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11FaultUntilDetail(reader) THEN
            EXIT CASE
          END IF
          IF WSHelper_CheckStaxSOAP11FaultDetail(reader) THEN
            #
            # STAX SOAP FAULT DESERIALIZE
            #
            CALL reader.nextSibling() # Skip SOAP detail
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndFault(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndBody(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndEnvelope(reader) THEN
            EXIT CASE
          END IF
          # End Streaming Response
          CALL response.endXmlResponse(reader)

        WHEN 200 # SOAP Result
          #
          # STAX SOAP RESPONSE
          #
          LET reader = response.beginXmlResponse() # Begin Streaming Response
          IF NOT WSHelper_ReadStaxSOAP11StartEnvelope(reader) THEN
            EXIT CASE
          END IF
          # Process SOAP headers 
          IF WSHelper_CheckStaxSOAP11Header(reader) AND NOT reader.isEmptyElement() THEN
            LET nb = 0
            CALL reader.nextTag()
            WHILE (reader.getEventType()=="START_ELEMENT")
              IF WSHelper_CheckStaxSOAP11HeaderActor(reader,uri) THEN
                LET mustUnderstand = WSHelper_GetStaxSOAP11HeaderMustUnderstand(reader)
                IF mustUnderstand = -1 THEN
                  CALL WSHelper_FillSOAP11WSError("Client","Invalid mustUnderstand value")
                  EXIT CASE
                END IF
                #
                # STAX SOAP RESPONSE HEADER DESERIALIZE
                #
                IF mustUnderstand THEN
                  CALL WSHelper_FillSOAP11WSError("MustUnderstand","Mandatory header block not understood")
                  EXIT CASE
                ELSE
                  CALL reader.nextSibling() # Skip header, not necessary
                END IF
              ELSE
                CALL reader.nextSibling() # Skip header, not intended to us
              END IF
            END WHILE
            IF NOT WSHelper_CheckStaxSOAP11Header(reader) THEN
              CALL WSHelper_FillSOAP11WSError("Client","No ending header tag found")
              EXIT CASE
            ELSE
              IF nb != 0 THEN
                CALL WSHelper_FillSOAP11WSError("Client","One or more headers are missing")
                EXIT CASE
              ELSE
                CALL reader.nextTag()
              END IF
            END IF
          ELSE
           IF reader.isEmptyElement() THEN
             CALL reader.nextTag()
           END IF
          END IF
          IF NOT WSHelper_ReadStaxSOAP11StartBody(reader) THEN
            EXIT CASE
          END IF
          # Retrieve SOAP Message taking soap:root attribute into account
          IF NOT WSHelper_RetrieveStaxSOAP11Message(reader) THEN
            EXIT CASE
          END IF
          #
          # STAX SOAP RESPONSE DESERIALIZE
          #
          CALL xml.Serializer.StaxToVariable(reader,fn_ret_saldos_disponibles_ley73Response)
          IF NOT WSHelper_ReadStaxSOAP11EndBody(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndEnvelope(reader) THEN
            EXIT CASE
          END IF
          # End Streaming Response
          CALL response.endXmlResponse(reader)
          LET wsstatus = 0

        WHEN 401 # HTTP Authentication
          IF retryAuth THEN
            CALL WSHelper_FillSOAP11WSError("Server","HTTP Error 401 ("||response.getStatusDescription()||")")
          ELSE
            LET retryAuth = TRUE
            LET retry = TRUE
          END IF

        WHEN 407 # Proxy Authentication
          IF retryProxy THEN
            CALL WSHelper_FillSOAP11WSError("Server","HTTP Error 407 ("||response.getStatusDescription()||")")
          ELSE
            LET retryProxy = TRUE
            LET retry = TRUE
          END IF

        OTHERWISE
          CALL WSHelper_FillSOAP11WSError("Server","HTTP Error "||response.getStatusCode()||" ("||response.getStatusDescription()||")")

      END CASE
    CATCH
      LET wsstatus = status
      CALL WSHelper_FillSOAP11WSError("Server",SQLCA.SQLERRM)
      RETURN wsstatus    
    END TRY

  # END LOOP
  END WHILE

  RETURN wsstatus

END FUNCTION


FUNCTION fn_ret_saldos_disponibles_ley73Request_g()
  DEFINE wsstatus   INTEGER
  DEFINE nb         INTEGER
  DEFINE writer     xml.StaxWriter

  #
  # CHECK PREVIOUS CALL  
  #
  IF fn_ret_saldos_disponibles_ley73HTTPReq IS NOT NULL AND fn_ret_saldos_disponibles_ley73HTTPResp IS NULL THEN
    # Request was sent but there was no response yet
    CALL WSHelper_FillSOAP11WSError("Client","Cannot issue a new request until previous response was received")
    RETURN -2 # waiting for the response
  ELSE
    IF retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Address.Uri IS NULL THEN
      IF ( FGL_GETENV("URL_RETWS_DISPONIBILIDAD_LEY73") ) THEN
          LET retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Address.Uri = FGL_GETENV("URL_RETWS_DISPONIBILIDAD_LEY73")
      ELSE
          DISPLAY "ERROR: No se encuentra la variable de entorno URL_RETWS_DISPONIBILIDAD_LEY73 o esta vacia. Se usara la URL http://10.90.8.132/disponibilidadLey73/ws/r/retiroSaldosDisponiblesLey73"
          LET retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Address.Uri = "http://10.90.8.132/disponibilidadLey73/ws/r/retiroSaldosDisponiblesLey73"
      END IF
    END IF
  END IF

  #
  # CREATE REQUEST
  #
  TRY
    LET fn_ret_saldos_disponibles_ley73HTTPReq = com.HTTPRequest.Create(retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Address.Uri)
    CALL fn_ret_saldos_disponibles_ley73HTTPReq.setMethod("POST")
    CALL fn_ret_saldos_disponibles_ley73HTTPReq.setCharset("UTF-8")
    CALL fn_ret_saldos_disponibles_ley73HTTPReq.setHeader("SOAPAction","\"fn_ret_saldos_disponibles_ley73\"")
    CALL WSHelper_SetRequestHeaders(fn_ret_saldos_disponibles_ley73HTTPReq, retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.Request.Headers)
    IF retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.Version IS NOT NULL THEN
      CALL fn_ret_saldos_disponibles_ley73HTTPReq.setVersion(retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.Version)
    END IF
    IF retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.Cookie IS NOT NULL THEN
      CALL fn_ret_saldos_disponibles_ley73HTTPReq.setHeader("Cookie",retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.Cookie)
    END IF
    IF retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.ConnectionTimeout <> 0 THEN
      CALL fn_ret_saldos_disponibles_ley73HTTPReq.setConnectionTimeout(retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.ConnectionTimeout)
    END IF
    IF retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.ReadWriteTimeout <> 0 THEN
      CALL fn_ret_saldos_disponibles_ley73HTTPReq.setTimeout(retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.ReadWriteTimeout)
    END IF
    IF retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.CompressRequest IS NOT NULL THEN
      CALL fn_ret_saldos_disponibles_ley73HTTPReq.setHeader("Content-Encoding",retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.CompressRequest)
    END IF
    CALL fn_ret_saldos_disponibles_ley73HTTPReq.setHeader("Accept-Encoding","gzip, deflate")
  CATCH
    LET wsstatus = STATUS
    CALL WSHelper_FillSOAP11WSError("Client","Cannot create HTTPRequest")
    LET fn_ret_saldos_disponibles_ley73HTTPReq = NULL
    RETURN wsstatus    
  END TRY

    #
    # Stax request
    #
    TRY
      LET writer = fn_ret_saldos_disponibles_ley73HTTPReq.beginXmlRequest()
      CALL WSHelper_WriteStaxSOAP11StartEnvelope(writer)
      CALL WSHelper_WriteStaxSOAP11StartBody(writer)
      #
      # STAX SOAP REQUEST SERIALIZE
      #
      CALL xml.Serializer.VariableToStax(fn_ret_saldos_disponibles_ley73Request,writer)
      CALL WSHelper_WriteStaxSOAP11EndBody(writer)
      CALL WSHelper_WriteStaxSOAP11EndEnvelope(writer)
      CALL fn_ret_saldos_disponibles_ley73HTTPReq.endXmlRequest(writer)
    CATCH
      LET wsstatus = STATUS
      CALL WSHelper_FillSOAP11WSError("Client",SQLCA.SQLERRM)
      LET fn_ret_saldos_disponibles_ley73HTTPReq = NULL
      RETURN wsstatus    
    END TRY

  #
  # PROCESS RESPONSE
  #
  TRY
    LET fn_ret_saldos_disponibles_ley73HTTPResp = fn_ret_saldos_disponibles_ley73HTTPReq.getAsyncResponse()
    RETURN 0 # SUCCESS
  CATCH
    LET wsstatus = STATUS
    CALL WSHelper_FillSOAP11WSError("Server",SQLCA.SQLERRM)
    LET fn_ret_saldos_disponibles_ley73HTTPReq = NULL
    RETURN wsstatus
  END TRY
END FUNCTION


FUNCTION fn_ret_saldos_disponibles_ley73Response_g()
  DEFINE wsstatus        INTEGER
  DEFINE nb              INTEGER
  DEFINE uri             STRING
  DEFINE setcookie       STRING
  DEFINE mustUnderstand  INTEGER
  DEFINE reader          xml.StaxReader

  LET wsstatus = -1

  LET uri = com.WebServiceEngine.GetOption("SoapModuleURI")
  #
  # CHECK PREVIOUS CALL  
  #
  IF fn_ret_saldos_disponibles_ley73HTTPReq IS NULL THEN
    # No request was sent
    CALL WSHelper_FillSOAP11WSError("Client","No request has been sent")
    RETURN -1    
  END IF

  TRY
    #
    # PROCESS RESPONSE
    #
    IF fn_ret_saldos_disponibles_ley73HTTPResp IS NULL THEN
      # Still no response, try again
      LET fn_ret_saldos_disponibles_ley73HTTPResp = fn_ret_saldos_disponibles_ley73HTTPReq.getAsyncResponse()
    END IF

    IF fn_ret_saldos_disponibles_ley73HTTPResp IS NULL THEN
      # We got no response, still waiting for
      CALL WSHelper_FillSOAP11WSError("Client","Response was not yet received")
      RETURN -2      
    END IF

      #
      # RETRIEVE SERVICE SESSION COOKIE
      #
      LET setcookie = fn_ret_saldos_disponibles_ley73HTTPResp.getHeader("Set-Cookie")
      IF setcookie IS NOT NULL THEN
        LET retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.Cookie = WSHelper_ExtractServerCookie(setcookie,retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Address.Uri)
      END IF

      #
      # RETRIEVE HTTP RESPONSE Headers
      #
      CALL WSHelper_SetResponseHeaders(fn_ret_saldos_disponibles_ley73HTTPResp, retiroSaldosDisponiblesLey73_retiroSaldosDisponiblesLey73PortTypeEndpoint.Binding.Response.Headers)
      CASE fn_ret_saldos_disponibles_ley73HTTPResp.getStatusCode()

        WHEN 500 # SOAP Fault
          #
          # STAX SOAP FAULT
          #
          LET reader = fn_ret_saldos_disponibles_ley73HTTPResp.beginXmlResponse() # Begin Streaming Response
          IF NOT WSHelper_ReadStaxSOAP11StartEnvelope(reader) THEN
            EXIT CASE
          END IF
          # Process SOAP headers 
          IF WSHelper_CheckStaxSOAP11Header(reader) AND NOT reader.isEmptyElement() THEN
            CALL reader.nextTag()
            WHILE (reader.getEventType()=="START_ELEMENT")
              IF WSHelper_CheckStaxSOAP11HeaderActor(reader,uri) THEN
                LET mustUnderstand = WSHelper_GetStaxSOAP11HeaderMustUnderstand(reader)
                IF mustUnderstand = -1 THEN
                  CALL WSHelper_FillSOAP11WSError("Client","Invalid mustUnderstand value")
                  EXIT CASE
                END IF
                IF mustUnderstand THEN
                  CALL WSHelper_FillSOAP11WSError("MustUnderstand","Mandatory header block not understood")
                  EXIT CASE
                ELSE
                  CALL reader.nextSibling() # Skip header, not necessary
                END IF
              ELSE
                CALL reader.nextSibling() # Skip header, not intended to us
              END IF
            END WHILE
            IF NOT WSHelper_CheckStaxSOAP11Header(reader) THEN
              CALL WSHelper_FillSOAP11WSError("Client","No ending header tag found")
              EXIT CASE
            ELSE
              CALL reader.nextTag()
            END IF
          ELSE
            IF WSHelper_CheckStaxSOAP11Header(reader) THEN
              CALL reader.nextSibling() # Skip SOAP headers
            END IF
          END IF
          IF NOT WSHelper_ReadStaxSOAP11StartBody(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11StartFault(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11FaultUntilDetail(reader) THEN
            EXIT CASE
          END IF
          IF WSHelper_CheckStaxSOAP11FaultDetail(reader) THEN
            #
            # STAX SOAP FAULT DESERIALIZE
            #
            CALL reader.nextSibling() # Skip SOAP detail
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndFault(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndBody(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndEnvelope(reader) THEN
            EXIT CASE
          END IF
          # End Streaming Response
          CALL fn_ret_saldos_disponibles_ley73HTTPResp.endXmlResponse(reader)

        WHEN 200 # SOAP Result
          #
          # STAX SOAP RESPONSE
          #
          LET reader = fn_ret_saldos_disponibles_ley73HTTPResp.beginXmlResponse() # Begin Streaming Response
          IF NOT WSHelper_ReadStaxSOAP11StartEnvelope(reader) THEN
            EXIT CASE
          END IF
          # Process SOAP headers 
          IF WSHelper_CheckStaxSOAP11Header(reader) AND NOT reader.isEmptyElement() THEN
            LET nb = 0
            CALL reader.nextTag()
            WHILE (reader.getEventType()=="START_ELEMENT")
              IF WSHelper_CheckStaxSOAP11HeaderActor(reader,uri) THEN
                LET mustUnderstand = WSHelper_GetStaxSOAP11HeaderMustUnderstand(reader)
                IF mustUnderstand = -1 THEN
                  CALL WSHelper_FillSOAP11WSError("Client","Invalid mustUnderstand value")
                  EXIT CASE
                END IF
                #
                # STAX SOAP RESPONSE HEADER DESERIALIZE
                #
                IF mustUnderstand THEN
                  CALL WSHelper_FillSOAP11WSError("MustUnderstand","Mandatory header block not understood")
                  EXIT CASE
                ELSE
                  CALL reader.nextSibling() # Skip header, not necessary
                END IF
              ELSE
                CALL reader.nextSibling() # Skip header, not intended to us
              END IF
            END WHILE
            IF NOT WSHelper_CheckStaxSOAP11Header(reader) THEN
              CALL WSHelper_FillSOAP11WSError("Client","No ending header tag found")
              EXIT CASE
            ELSE
              IF nb != 0 THEN
                CALL WSHelper_FillSOAP11WSError("Client","One or more headers are missing")
                EXIT CASE
              ELSE
                CALL reader.nextTag()
              END IF
            END IF
          ELSE
           IF reader.isEmptyElement() THEN
             CALL reader.nextTag()
           END IF
          END IF
          IF NOT WSHelper_ReadStaxSOAP11StartBody(reader) THEN
            EXIT CASE
          END IF
          # Retrieve SOAP Message taking soap:root attribute into account
          IF NOT WSHelper_RetrieveStaxSOAP11Message(reader) THEN
            EXIT CASE
          END IF
          #
          # STAX SOAP RESPONSE DESERIALIZE
          #
          CALL xml.Serializer.StaxToVariable(reader,fn_ret_saldos_disponibles_ley73Response)
          IF NOT WSHelper_ReadStaxSOAP11EndBody(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndEnvelope(reader) THEN
            EXIT CASE
          END IF
          # End Streaming Response
          CALL fn_ret_saldos_disponibles_ley73HTTPResp.endXmlResponse(reader)
          LET wsstatus = 0

        OTHERWISE
          CALL WSHelper_FillSOAP11WSError("Server","HTTP Error "||fn_ret_saldos_disponibles_ley73HTTPResp.getStatusCode()||" ("||fn_ret_saldos_disponibles_ley73HTTPResp.getStatusDescription()||")")

      END CASE
    CATCH
      LET wsstatus = status
      CALL WSHelper_FillSOAP11WSError("Server",SQLCA.SQLERRM)
    END TRY

  #
  # RESET VARIABLES
  #
  LET fn_ret_saldos_disponibles_ley73HTTPReq = NULL
  LET fn_ret_saldos_disponibles_ley73HTTPResp = NULL
  RETURN wsstatus

END FUNCTION


