#-------------------------------------------------------------------------------
# File: PRTW08.4gl
# GENERATED BY fglwsdl 141859
#-------------------------------------------------------------------------------
# THIS FILE WAS GENERATED. DO NOT MODIFY.
#-------------------------------------------------------------------------------


IMPORT FGL WSHelper
IMPORT com
IMPORT xml


GLOBALS "PRTW08.inc"



#-------------------------------------------------------------------------------
# Service: TransferenciaRecursosInfonavitExt
# Port:    TransferenciaRecursosInfonavitExtPort
# Server:  http://localhost:7001/PortabilidadInfonavitFovissste/Externo/TransferenciaRecursosInfonavit/
#-------------------------------------------------------------------------------

PRIVATE DEFINE solicitudTransferenciaInfonavitHTTPReq     com.HTTPRequest
PRIVATE DEFINE solicitudTransferenciaInfonavitHTTPResp    com.HTTPResponse

#-------------------------------------------------------------------------------

#
# Operation: solicitudTransferenciaInfonavit
#
#
# FUNCTION: solicitudTransferenciaInfonavit_g
#   RETURNING: soapStatus
#   INPUT: GLOBAL ns1solicitudTransferenciaInfonavitRequest
#   OUTPUT: GLOBAL ns1solicitudTransferenciaInfonavitResponse
#
FUNCTION solicitudTransferenciaInfonavit_g()
  DEFINE wsstatus   INTEGER
  DEFINE retryAuth  INTEGER
  DEFINE retryProxy INTEGER
  DEFINE retry      INTEGER
  DEFINE nb         INTEGER
  DEFINE uri        STRING
  DEFINE setcookie  STRING
  DEFINE mustUnderstand INTEGER
  DEFINE request    com.HTTPRequest
  DEFINE response   com.HTTPResponse
  DEFINE writer     xml.StaxWriter
  DEFINE reader     xml.StaxReader

  #
  # INIT VARIABLES
  #
  LET wsstatus = -1
  LET retryAuth = FALSE
  LET retryProxy = FALSE
  LET retry = TRUE
  LET uri = com.WebServiceEngine.GetOption("SoapModuleURI")

  IF TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Address.Uri IS NULL THEN
    LET TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Address.Uri = "http://localhost:7001/PortabilidadInfonavitFovissste/Externo/TransferenciaRecursosInfonavit/"
  END IF

  #
  # CREATE REQUEST
  #
  TRY
    LET request = com.HTTPRequest.Create(TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Address.Uri)
    CALL request.setMethod("POST")
    CALL request.setCharset("UTF-8")
    CALL request.setHeader("SOAPAction","\"http://www.procesar.com.mx/PortabilidadInfonavitFovissste/Externo/TransferenciaRecursos/solicitudTransferenciaInfonavit/\"")
    CALL WSHelper_SetRequestHeaders(request, TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.Request.Headers)
    IF TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.Version IS NOT NULL THEN
      CALL request.setVersion(TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.Version)
    END IF
    IF TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.Cookie IS NOT NULL THEN
      CALL request.setHeader("Cookie",TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.Cookie)
    END IF
    IF TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.ConnectionTimeout <> 0 THEN
      CALL request.setConnectionTimeout(TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.ConnectionTimeout)
    END IF
    IF TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.ReadWriteTimeout <> 0 THEN
      CALL request.setTimeout(TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.ReadWriteTimeout)
    END IF
    IF TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.CompressRequest IS NOT NULL THEN
      CALL request.setHeader("Content-Encoding",TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.CompressRequest)
    END IF
    CALL request.setHeader("Accept-Encoding","gzip, deflate")
  CATCH
    LET wsstatus = STATUS
    CALL WSHelper_FillSOAP11WSError("Client","Cannot create HTTPRequest")
    RETURN wsstatus
  END TRY

  # START LOOP
  WHILE retry
    LET retry = FALSE

    #
    # Stax request
    #
    TRY
      LET writer = request.beginXmlRequest()
      CALL WSHelper_WriteStaxSOAP11StartEnvelope(writer)
      CALL WSHelper_WriteStaxSOAP11StartBody(writer)
      #
      # STAX SOAP REQUEST SERIALIZE
      #
      CALL xml.Serializer.VariableToStax(ns1solicitudTransferenciaInfonavitRequest,writer)
      CALL WSHelper_WriteStaxSOAP11EndBody(writer)
      CALL WSHelper_WriteStaxSOAP11EndEnvelope(writer)
      CALL request.endXmlRequest(writer)
    CATCH
      LET wsstatus = STATUS
      CALL WSHelper_FillSOAP11WSError("Client",SQLCA.SQLERRM)
      RETURN wsstatus
    END TRY

    #
    # PROCESS RESPONSE
    #
    TRY
      LET response = request.getResponse()

      #
      # RETRIEVE SERVICE SESSION COOKIE
      #
      LET setcookie = response.getHeader("Set-Cookie")
      IF setcookie IS NOT NULL THEN
        LET TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.Cookie = WSHelper_ExtractServerCookie(setcookie,TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Address.Uri)
      END IF

      #
      # RETRIEVE HTTP RESPONSE Headers
      #
      CALL WSHelper_SetResponseHeaders(response, TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.Response.Headers)
      CASE response.getStatusCode()

        WHEN 500 # SOAP Fault
          #
          # STAX SOAP FAULT
          #
          LET reader = response.beginXmlResponse() # Begin Streaming Response
          IF NOT WSHelper_ReadStaxSOAP11StartEnvelope(reader) THEN
            EXIT CASE
          END IF
          # Process SOAP headers 
          IF WSHelper_CheckStaxSOAP11Header(reader) AND NOT reader.isEmptyElement() THEN
            CALL reader.nextTag()
            WHILE (reader.getEventType()=="START_ELEMENT")
              IF WSHelper_CheckStaxSOAP11HeaderActor(reader,uri) THEN
                LET mustUnderstand = WSHelper_GetStaxSOAP11HeaderMustUnderstand(reader)
                IF mustUnderstand = -1 THEN
                  CALL WSHelper_FillSOAP11WSError("Client","Invalid mustUnderstand value")
                  EXIT CASE
                END IF
                IF mustUnderstand THEN
                  CALL WSHelper_FillSOAP11WSError("MustUnderstand","Mandatory header block not understood")
                  EXIT CASE
                ELSE
                  CALL reader.nextSibling() # Skip header, not necessary
                END IF
              ELSE
                CALL reader.nextSibling() # Skip header, not intended to us
              END IF
            END WHILE
            IF NOT WSHelper_CheckStaxSOAP11Header(reader) THEN
              CALL WSHelper_FillSOAP11WSError("Client","No ending header tag found")
              EXIT CASE
            ELSE
              CALL reader.nextTag()
            END IF
          ELSE
            IF WSHelper_CheckStaxSOAP11Header(reader) THEN
              CALL reader.nextSibling() # Skip SOAP headers
            END IF
          END IF
          IF NOT WSHelper_ReadStaxSOAP11StartBody(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11StartFault(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11FaultUntilDetail(reader) THEN
            EXIT CASE
          END IF
          IF WSHelper_CheckStaxSOAP11FaultDetail(reader) THEN
            #
            # STAX SOAP FAULT DESERIALIZE
            #
            CALL reader.nextSibling() # Skip SOAP detail
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndFault(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndBody(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndEnvelope(reader) THEN
            EXIT CASE
          END IF
          # End Streaming Response
          CALL response.endXmlResponse(reader)

        WHEN 200 # SOAP Result
          #
          # STAX SOAP RESPONSE
          #
          LET reader = response.beginXmlResponse() # Begin Streaming Response
          IF NOT WSHelper_ReadStaxSOAP11StartEnvelope(reader) THEN
            EXIT CASE
          END IF
          # Process SOAP headers 
          IF WSHelper_CheckStaxSOAP11Header(reader) AND NOT reader.isEmptyElement() THEN
            LET nb = 0
            CALL reader.nextTag()
            WHILE (reader.getEventType()=="START_ELEMENT")
              IF WSHelper_CheckStaxSOAP11HeaderActor(reader,uri) THEN
                LET mustUnderstand = WSHelper_GetStaxSOAP11HeaderMustUnderstand(reader)
                IF mustUnderstand = -1 THEN
                  CALL WSHelper_FillSOAP11WSError("Client","Invalid mustUnderstand value")
                  EXIT CASE
                END IF
                #
                # STAX SOAP RESPONSE HEADER DESERIALIZE
                #
                IF mustUnderstand THEN
                  CALL WSHelper_FillSOAP11WSError("MustUnderstand","Mandatory header block not understood")
                  EXIT CASE
                ELSE
                  CALL reader.nextSibling() # Skip header, not necessary
                END IF
              ELSE
                CALL reader.nextSibling() # Skip header, not intended to us
              END IF
            END WHILE
            IF NOT WSHelper_CheckStaxSOAP11Header(reader) THEN
              CALL WSHelper_FillSOAP11WSError("Client","No ending header tag found")
              EXIT CASE
            ELSE
              IF nb != 0 THEN
                CALL WSHelper_FillSOAP11WSError("Client","One or more headers are missing")
                EXIT CASE
              ELSE
                CALL reader.nextTag()
              END IF
            END IF
          ELSE
           IF reader.isEmptyElement() THEN
             CALL reader.nextTag()
           END IF
          END IF
          IF NOT WSHelper_ReadStaxSOAP11StartBody(reader) THEN
            EXIT CASE
          END IF
          # Retrieve SOAP Message taking soap:root attribute into account
          IF NOT WSHelper_RetrieveStaxSOAP11Message(reader) THEN
            EXIT CASE
          END IF
          #
          # STAX SOAP RESPONSE DESERIALIZE
          #
          CALL xml.Serializer.StaxToVariable(reader,ns1solicitudTransferenciaInfonavitResponse)
          IF NOT WSHelper_ReadStaxSOAP11EndBody(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndEnvelope(reader) THEN
            EXIT CASE
          END IF
          # End Streaming Response
          CALL response.endXmlResponse(reader)
          LET wsstatus = 0

        WHEN 401 # HTTP Authentication
          IF retryAuth THEN
            CALL WSHelper_FillSOAP11WSError("Server","HTTP Error 401 ("||response.getStatusDescription()||")")
          ELSE
            LET retryAuth = TRUE
            LET retry = TRUE
          END IF

        WHEN 407 # Proxy Authentication
          IF retryProxy THEN
            CALL WSHelper_FillSOAP11WSError("Server","HTTP Error 407 ("||response.getStatusDescription()||")")
          ELSE
            LET retryProxy = TRUE
            LET retry = TRUE
          END IF

        OTHERWISE
          CALL WSHelper_FillSOAP11WSError("Server","HTTP Error "||response.getStatusCode()||" ("||response.getStatusDescription()||")")

      END CASE
    CATCH
      LET wsstatus = status
      CALL WSHelper_FillSOAP11WSError("Server",SQLCA.SQLERRM)
      RETURN wsstatus
    END TRY

  # END LOOP
  END WHILE

  RETURN wsstatus

END FUNCTION


FUNCTION solicitudTransferenciaInfonavitRequest_g()
  DEFINE wsstatus   INTEGER
  DEFINE writer     xml.StaxWriter

  #
  # CHECK PREVIOUS CALL  
  #
  IF solicitudTransferenciaInfonavitHTTPReq IS NOT NULL AND solicitudTransferenciaInfonavitHTTPResp IS NULL THEN
    # Request was sent but there was no response yet
    CALL WSHelper_FillSOAP11WSError("Client","Cannot issue a new request until previous response was received")
    RETURN -2 # waiting for the response
  ELSE
    IF TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Address.Uri IS NULL THEN
      LET TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Address.Uri = "http://localhost:7001/PortabilidadInfonavitFovissste/Externo/TransferenciaRecursosInfonavit/"
    END IF
  END IF

  #
  # CREATE REQUEST
  #
  TRY
    LET solicitudTransferenciaInfonavitHTTPReq = com.HTTPRequest.Create(TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Address.Uri)
    CALL solicitudTransferenciaInfonavitHTTPReq.setMethod("POST")
    CALL solicitudTransferenciaInfonavitHTTPReq.setCharset("UTF-8")
    CALL solicitudTransferenciaInfonavitHTTPReq.setHeader("SOAPAction","\"http://www.procesar.com.mx/PortabilidadInfonavitFovissste/Externo/TransferenciaRecursos/solicitudTransferenciaInfonavit/\"")
    CALL WSHelper_SetRequestHeaders(solicitudTransferenciaInfonavitHTTPReq, TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.Request.Headers)
    IF TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.Version IS NOT NULL THEN
      CALL solicitudTransferenciaInfonavitHTTPReq.setVersion(TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.Version)
    END IF
    IF TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.Cookie IS NOT NULL THEN
      CALL solicitudTransferenciaInfonavitHTTPReq.setHeader("Cookie",TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.Cookie)
    END IF
    IF TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.ConnectionTimeout <> 0 THEN
      CALL solicitudTransferenciaInfonavitHTTPReq.setConnectionTimeout(TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.ConnectionTimeout)
    END IF
    IF TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.ReadWriteTimeout <> 0 THEN
      CALL solicitudTransferenciaInfonavitHTTPReq.setTimeout(TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.ReadWriteTimeout)
    END IF
    IF TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.CompressRequest IS NOT NULL THEN
      CALL solicitudTransferenciaInfonavitHTTPReq.setHeader("Content-Encoding",TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.CompressRequest)
    END IF
    CALL solicitudTransferenciaInfonavitHTTPReq.setHeader("Accept-Encoding","gzip, deflate")
  CATCH
    LET wsstatus = STATUS
    CALL WSHelper_FillSOAP11WSError("Client","Cannot create HTTPRequest")
    LET solicitudTransferenciaInfonavitHTTPReq = NULL
    RETURN wsstatus
  END TRY

    #
    # Stax request
    #
    TRY
      LET writer = solicitudTransferenciaInfonavitHTTPReq.beginXmlRequest()
      CALL WSHelper_WriteStaxSOAP11StartEnvelope(writer)
      CALL WSHelper_WriteStaxSOAP11StartBody(writer)
      #
      # STAX SOAP REQUEST SERIALIZE
      #
      CALL xml.Serializer.VariableToStax(ns1solicitudTransferenciaInfonavitRequest,writer)
      CALL WSHelper_WriteStaxSOAP11EndBody(writer)
      CALL WSHelper_WriteStaxSOAP11EndEnvelope(writer)
      CALL solicitudTransferenciaInfonavitHTTPReq.endXmlRequest(writer)
    CATCH
      LET wsstatus = STATUS
      CALL WSHelper_FillSOAP11WSError("Client",SQLCA.SQLERRM)
      LET solicitudTransferenciaInfonavitHTTPReq = NULL
      RETURN wsstatus
    END TRY

  #
  # PROCESS RESPONSE
  #
  TRY
    LET solicitudTransferenciaInfonavitHTTPResp = solicitudTransferenciaInfonavitHTTPReq.getAsyncResponse()
    RETURN 0 # SUCCESS
  CATCH
    LET wsstatus = STATUS
    CALL WSHelper_FillSOAP11WSError("Server",SQLCA.SQLERRM)
    LET solicitudTransferenciaInfonavitHTTPReq = NULL
    RETURN wsstatus
  END TRY
END FUNCTION


FUNCTION solicitudTransferenciaInfonavitResponse_g()
  DEFINE wsstatus        INTEGER
  DEFINE nb              INTEGER
  DEFINE uri             STRING
  DEFINE setcookie       STRING
  DEFINE mustUnderstand  INTEGER
  DEFINE reader          xml.StaxReader

  LET wsstatus = -1

  LET uri = com.WebServiceEngine.GetOption("SoapModuleURI")
  #
  # CHECK PREVIOUS CALL  
  #
  IF solicitudTransferenciaInfonavitHTTPReq IS NULL THEN
    # No request was sent
    CALL WSHelper_FillSOAP11WSError("Client","No request has been sent")
    RETURN -1
  END IF

  TRY
    #
    # PROCESS RESPONSE
    #
    IF solicitudTransferenciaInfonavitHTTPResp IS NULL THEN
      # Still no response, try again
      LET solicitudTransferenciaInfonavitHTTPResp = solicitudTransferenciaInfonavitHTTPReq.getAsyncResponse()
    END IF

    IF solicitudTransferenciaInfonavitHTTPResp IS NULL THEN
      # We got no response, still waiting for
      CALL WSHelper_FillSOAP11WSError("Client","Response was not yet received")
      RETURN -2
    END IF

      #
      # RETRIEVE SERVICE SESSION COOKIE
      #
      LET setcookie = solicitudTransferenciaInfonavitHTTPResp.getHeader("Set-Cookie")
      IF setcookie IS NOT NULL THEN
        LET TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.Cookie = WSHelper_ExtractServerCookie(setcookie,TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Address.Uri)
      END IF

      #
      # RETRIEVE HTTP RESPONSE Headers
      #
      CALL WSHelper_SetResponseHeaders(solicitudTransferenciaInfonavitHTTPResp, TransferenciaRecursosInfonavitExt_TransferenciaRecursosInfonavitExtPortEndpoint.Binding.Response.Headers)
      CASE solicitudTransferenciaInfonavitHTTPResp.getStatusCode()

        WHEN 500 # SOAP Fault
          #
          # STAX SOAP FAULT
          #
          LET reader = solicitudTransferenciaInfonavitHTTPResp.beginXmlResponse() # Begin Streaming Response
          IF NOT WSHelper_ReadStaxSOAP11StartEnvelope(reader) THEN
            EXIT CASE
          END IF
          # Process SOAP headers 
          IF WSHelper_CheckStaxSOAP11Header(reader) AND NOT reader.isEmptyElement() THEN
            CALL reader.nextTag()
            WHILE (reader.getEventType()=="START_ELEMENT")
              IF WSHelper_CheckStaxSOAP11HeaderActor(reader,uri) THEN
                LET mustUnderstand = WSHelper_GetStaxSOAP11HeaderMustUnderstand(reader)
                IF mustUnderstand = -1 THEN
                  CALL WSHelper_FillSOAP11WSError("Client","Invalid mustUnderstand value")
                  EXIT CASE
                END IF
                IF mustUnderstand THEN
                  CALL WSHelper_FillSOAP11WSError("MustUnderstand","Mandatory header block not understood")
                  EXIT CASE
                ELSE
                  CALL reader.nextSibling() # Skip header, not necessary
                END IF
              ELSE
                CALL reader.nextSibling() # Skip header, not intended to us
              END IF
            END WHILE
            IF NOT WSHelper_CheckStaxSOAP11Header(reader) THEN
              CALL WSHelper_FillSOAP11WSError("Client","No ending header tag found")
              EXIT CASE
            ELSE
              CALL reader.nextTag()
            END IF
          ELSE
            IF WSHelper_CheckStaxSOAP11Header(reader) THEN
              CALL reader.nextSibling() # Skip SOAP headers
            END IF
          END IF
          IF NOT WSHelper_ReadStaxSOAP11StartBody(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11StartFault(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11FaultUntilDetail(reader) THEN
            EXIT CASE
          END IF
          IF WSHelper_CheckStaxSOAP11FaultDetail(reader) THEN
            #
            # STAX SOAP FAULT DESERIALIZE
            #
            CALL reader.nextSibling() # Skip SOAP detail
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndFault(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndBody(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndEnvelope(reader) THEN
            EXIT CASE
          END IF
          # End Streaming Response
          CALL solicitudTransferenciaInfonavitHTTPResp.endXmlResponse(reader)

        WHEN 200 # SOAP Result
          #
          # STAX SOAP RESPONSE
          #
          LET reader = solicitudTransferenciaInfonavitHTTPResp.beginXmlResponse() # Begin Streaming Response
          IF NOT WSHelper_ReadStaxSOAP11StartEnvelope(reader) THEN
            EXIT CASE
          END IF
          # Process SOAP headers 
          IF WSHelper_CheckStaxSOAP11Header(reader) AND NOT reader.isEmptyElement() THEN
            LET nb = 0
            CALL reader.nextTag()
            WHILE (reader.getEventType()=="START_ELEMENT")
              IF WSHelper_CheckStaxSOAP11HeaderActor(reader,uri) THEN
                LET mustUnderstand = WSHelper_GetStaxSOAP11HeaderMustUnderstand(reader)
                IF mustUnderstand = -1 THEN
                  CALL WSHelper_FillSOAP11WSError("Client","Invalid mustUnderstand value")
                  EXIT CASE
                END IF
                #
                # STAX SOAP RESPONSE HEADER DESERIALIZE
                #
                IF mustUnderstand THEN
                  CALL WSHelper_FillSOAP11WSError("MustUnderstand","Mandatory header block not understood")
                  EXIT CASE
                ELSE
                  CALL reader.nextSibling() # Skip header, not necessary
                END IF
              ELSE
                CALL reader.nextSibling() # Skip header, not intended to us
              END IF
            END WHILE
            IF NOT WSHelper_CheckStaxSOAP11Header(reader) THEN
              CALL WSHelper_FillSOAP11WSError("Client","No ending header tag found")
              EXIT CASE
            ELSE
              IF nb != 0 THEN
                CALL WSHelper_FillSOAP11WSError("Client","One or more headers are missing")
                EXIT CASE
              ELSE
                CALL reader.nextTag()
              END IF
            END IF
          ELSE
           IF reader.isEmptyElement() THEN
             CALL reader.nextTag()
           END IF
          END IF
          IF NOT WSHelper_ReadStaxSOAP11StartBody(reader) THEN
            EXIT CASE
          END IF
          # Retrieve SOAP Message taking soap:root attribute into account
          IF NOT WSHelper_RetrieveStaxSOAP11Message(reader) THEN
            EXIT CASE
          END IF
          #
          # STAX SOAP RESPONSE DESERIALIZE
          #
          CALL xml.Serializer.StaxToVariable(reader,ns1solicitudTransferenciaInfonavitResponse)
          IF NOT WSHelper_ReadStaxSOAP11EndBody(reader) THEN
            EXIT CASE
          END IF
          IF NOT WSHelper_ReadStaxSOAP11EndEnvelope(reader) THEN
            EXIT CASE
          END IF
          # End Streaming Response
          CALL solicitudTransferenciaInfonavitHTTPResp.endXmlResponse(reader)
          LET wsstatus = 0

        OTHERWISE
          CALL WSHelper_FillSOAP11WSError("Server","HTTP Error "||solicitudTransferenciaInfonavitHTTPResp.getStatusCode()||" ("||solicitudTransferenciaInfonavitHTTPResp.getStatusDescription()||")")

      END CASE
    CATCH
      LET wsstatus = status
      CALL WSHelper_FillSOAP11WSError("Server",SQLCA.SQLERRM)
    END TRY

  #
  # RESET VARIABLES
  #
  LET solicitudTransferenciaInfonavitHTTPReq = NULL
  LET solicitudTransferenciaInfonavitHTTPResp = NULL
  RETURN wsstatus

END FUNCTION


